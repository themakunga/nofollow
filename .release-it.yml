git:
  commitMessage: "chore: relase v${version}"
  tag: true
  push: false
  pushArgs:
    - --follow-tags
  requireCleanWorkingDir: true
github:
  release: true
plugins:
  '@release-it/conventional-changelog':
    preset: angular
    infile: CHANGELOG.md
npm:
  publish: false

